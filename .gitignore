# build artifacts / reports
/evalgo
eval-report.json
*.out
*.test

# agentbench: built binary, caches, filled-in targets (may hold keys), reports
agentbench/agent/benchagent
agentbench/.cache/
agentbench/targets.cloud.json
agentbench/report*.json

# secrets / local env — never commit
.env
.env.*
*.key

# editor / OS
.DS_Store
.idea/
.vscode/
