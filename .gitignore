# build artifacts / reports
/evalgo
eval-report.json
*.out
*.test

# agent-bench harness: built binary, caches, filled-in targets (may hold keys), reports
examples/agent-bench/agent/benchagent
examples/agent-bench/.cache/
examples/agent-bench/targets.cloud.json
examples/agent-bench/report*.json

# secrets / local env — never commit
.env
.env.*
*.key

# editor / OS
.DS_Store
.idea/
.vscode/
